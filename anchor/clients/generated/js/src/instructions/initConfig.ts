/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { NFT_GIFTER_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INIT_CONFIG_DISCRIMINATOR = new Uint8Array([
  23, 235, 115, 232, 168, 96, 1, 231,
]);

export function getInitConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_CONFIG_DISCRIMINATOR);
}

export type InitConfigInstruction<
  TProgram extends string = typeof NFT_GIFTER_PROGRAM_ADDRESS,
  TAccountOwner extends string | IAccountMeta<string> = string,
  TAccountConfig extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            IAccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  purchasePriceLamports: bigint;
  claimPriceLamports: bigint;
  tokensPerClaim: bigint;
  bump: number;
};

export type InitConfigInstructionDataArgs = {
  purchasePriceLamports: number | bigint;
  claimPriceLamports: number | bigint;
  tokensPerClaim: number | bigint;
  bump: number;
};

export function getInitConfigInstructionDataEncoder(): Encoder<InitConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['purchasePriceLamports', getU64Encoder()],
      ['claimPriceLamports', getU64Encoder()],
      ['tokensPerClaim', getU64Encoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_CONFIG_DISCRIMINATOR })
  );
}

export function getInitConfigInstructionDataDecoder(): Decoder<InitConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['purchasePriceLamports', getU64Decoder()],
    ['claimPriceLamports', getU64Decoder()],
    ['tokensPerClaim', getU64Decoder()],
    ['bump', getU8Decoder()],
  ]);
}

export function getInitConfigInstructionDataCodec(): Codec<
  InitConfigInstructionDataArgs,
  InitConfigInstructionData
> {
  return combineCodec(
    getInitConfigInstructionDataEncoder(),
    getInitConfigInstructionDataDecoder()
  );
}

export type InitConfigAsyncInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  purchasePriceLamports: InitConfigInstructionDataArgs['purchasePriceLamports'];
  claimPriceLamports: InitConfigInstructionDataArgs['claimPriceLamports'];
  tokensPerClaim: InitConfigInstructionDataArgs['tokensPerClaim'];
  bump: InitConfigInstructionDataArgs['bump'];
};

export async function getInitConfigInstructionAsync<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NFT_GIFTER_PROGRAM_ADDRESS,
>(
  input: InitConfigAsyncInput<
    TAccountOwner,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NFT_GIFTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitConfigInstructionDataEncoder().encode(
      args as InitConfigInstructionDataArgs
    ),
  } as InitConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountSystemProgram
  >;

  return instruction;
}

export type InitConfigInput<
  TAccountOwner extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  purchasePriceLamports: InitConfigInstructionDataArgs['purchasePriceLamports'];
  claimPriceLamports: InitConfigInstructionDataArgs['claimPriceLamports'];
  tokensPerClaim: InitConfigInstructionDataArgs['tokensPerClaim'];
  bump: InitConfigInstructionDataArgs['bump'];
};

export function getInitConfigInstruction<
  TAccountOwner extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NFT_GIFTER_PROGRAM_ADDRESS,
>(
  input: InitConfigInput<TAccountOwner, TAccountConfig, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): InitConfigInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NFT_GIFTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitConfigInstructionDataEncoder().encode(
      args as InitConfigInstructionDataArgs
    ),
  } as InitConfigInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountConfig,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedInitConfigInstruction<
  TProgram extends string = typeof NFT_GIFTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    config: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitConfigInstructionData;
};

export function parseInitConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedInitConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitConfigInstructionDataDecoder().decode(instruction.data),
  };
}
