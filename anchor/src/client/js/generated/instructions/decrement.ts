/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { NFTGIFTER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DECREMENT_DISCRIMINATOR = new Uint8Array([
  106, 227, 168, 59, 248, 27, 150, 101,
]);

export function getDecrementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DECREMENT_DISCRIMINATOR);
}

export type DecrementInstruction<
  TProgram extends string = typeof NFTGIFTER_PROGRAM_ADDRESS,
  TAccountNftgifter extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountNftgifter extends string
        ? WritableAccount<TAccountNftgifter>
        : TAccountNftgifter,
      ...TRemainingAccounts,
    ]
  >;

export type DecrementInstructionData = { discriminator: ReadonlyUint8Array };

export type DecrementInstructionDataArgs = {};

export function getDecrementInstructionDataEncoder(): Encoder<DecrementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DECREMENT_DISCRIMINATOR })
  );
}

export function getDecrementInstructionDataDecoder(): Decoder<DecrementInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDecrementInstructionDataCodec(): Codec<
  DecrementInstructionDataArgs,
  DecrementInstructionData
> {
  return combineCodec(
    getDecrementInstructionDataEncoder(),
    getDecrementInstructionDataDecoder()
  );
}

export type DecrementInput<TAccountNftgifter extends string = string> = {
  nftgifter: Address<TAccountNftgifter>;
};

export function getDecrementInstruction<
  TAccountNftgifter extends string,
  TProgramAddress extends Address = typeof NFTGIFTER_PROGRAM_ADDRESS,
>(
  input: DecrementInput<TAccountNftgifter>,
  config?: { programAddress?: TProgramAddress }
): DecrementInstruction<TProgramAddress, TAccountNftgifter> {
  // Program address.
  const programAddress = config?.programAddress ?? NFTGIFTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    nftgifter: { value: input.nftgifter ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.nftgifter)],
    programAddress,
    data: getDecrementInstructionDataEncoder().encode({}),
  } as DecrementInstruction<TProgramAddress, TAccountNftgifter>;

  return instruction;
}

export type ParsedDecrementInstruction<
  TProgram extends string = typeof NFTGIFTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    nftgifter: TAccountMetas[0];
  };
  data: DecrementInstructionData;
};

export function parseDecrementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedDecrementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      nftgifter: getNextAccount(),
    },
    data: getDecrementInstructionDataDecoder().decode(instruction.data),
  };
}
